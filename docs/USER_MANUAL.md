# User Manual

Connect your logging software to the rbn-dxcluster host using Telnet port `7373`. Enter your callsign when prompted. Use `help` to view supported commands. Start with `set/band 20m` and `set/mode CW` if the full RBN feed is too busy.
